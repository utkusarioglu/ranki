export const CARD_CONFIG = `
tags:
  - exact: +r:scheme-light
    config:
      design:
        scheme: light
  - exact: +r:theme-print
    config:
      design:
        theme: print
`.trim();

export const TEMPLATE_CONFIG = `
decks:
  - exact: Tests::Test
    config:
      aa: true 
`.trim();
