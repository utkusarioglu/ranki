import type { GeometryEventTypes } from "./types/geometry-events.constructor.types.mjs";

export const DEFAULT_EVENT_SETTINGS: GeometryEventTypes = {
  hover: false,
};

export const ACTION_TIME_SEPARATOR = "/";
